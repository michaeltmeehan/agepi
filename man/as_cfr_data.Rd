% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cfr_data.R
\name{as_cfr_data}
\alias{as_cfr_data}
\title{Convert cumulative flow output for CFR or severity analysis}
\usage{
as_cfr_data(
  simulation_output,
  cases,
  deaths,
  cumulative = TRUE,
  by_age_group = FALSE
)
}
\arguments{
\item{simulation_output}{Output returned by \code{\link[=simulate_deterministic]{simulate_deterministic()}} or
\code{\link[=simulate_stochastic]{simulate_stochastic()}} when \code{cumulative_flows} was supplied.}

\item{cases}{Flow selector for cases. A character scalar can match either
\code{cumulative_name} or \code{transition_id}. A list can specify \code{cumulative_name},
\code{transition_id}, or both.}

\item{deaths}{Flow selector for deaths, using the same format as \code{cases}.}

\item{cumulative}{Logical. If \code{TRUE}, selected cumulative flow values are
converted to interval increments. If \code{FALSE}, values are returned as-is.}

\item{by_age_group}{Logical. If \code{TRUE}, return one row per time and age group.
If \code{FALSE}, aggregate selected flows across age groups.}
}
\value{
A data frame with columns \code{time}, \code{cases}, and \code{deaths}. When
\code{by_age_group = TRUE}, an \code{age_group} column is also included.
}
\description{
Converts selected cumulative flow outputs from \code{\link[=simulate_deterministic]{simulate_deterministic()}} or
\code{\link[=simulate_stochastic]{simulate_stochastic()}} into a simple time series with cases and deaths. This
helper does not import or require \code{cfr}; it only prepares an ordinary
\code{data.frame} that can be passed to downstream tools.
}
\examples{
ages <- AgeStructure(c("0-4", "5+"), c(0, 5), c(4, Inf))
initial_state <- data.frame(
  compartment = rep(c("S", "I", "R"), each = 2),
  age_group = rep(ages$age_groups, times = 3),
  value = c(99, 198, 1, 2, 0, 0)
)
output <- simulate_deterministic(
  initial_state = initial_state,
  times = 0:2,
  model = SIRModel(gamma = 0.2),
  age_structure = ages,
  contact_matrix = diag(2),
  beta = 0.05,
  method = "euler",
  cumulative_flows = list(
    cases = list(from = "S", to = "I"),
    deaths = list(from = "I", to = "R")
  )
)
as_cfr_data(output, cases = "cases", deaths = "deaths")
}
