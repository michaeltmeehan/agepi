% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demographic_residuals.R
\name{residual_to_migration_schedule}
\alias{residual_to_migration_schedule}
\title{Convert implied residual diagnostics to a migration schedule}
\usage{
residual_to_migration_schedule(
  residual,
  age_structure,
  use = c("count", "rate")
)
}
\arguments{
\item{residual}{Data frame returned by \code{\link[=implied_demographic_residual]{implied_demographic_residual()}}, or a
data frame with required columns \code{time_start}, \code{time_end}, \code{dt},
\code{age_group}, \code{residual_count}, and \code{residual_rate}.}

\item{age_structure}{Age structure validated by \code{\link[=validate_age_structure]{validate_age_structure()}}.
Residual age groups must match this age structure exactly.}

\item{use}{Which residual convention to convert: \code{"count"} converts
interval residual counts to per-time \code{migration_count} flows via
\code{residual_count / dt}; \code{"rate"} uses \code{residual_rate} directly as
\code{migration_rate}.}
}
\value{
An \code{agepi_migration_schedule} object.
}
\description{
Converts output from \code{\link[=implied_demographic_residual]{implied_demographic_residual()}} into a
\code{\link[=MigrationSchedule]{MigrationSchedule()}} using an explicit Euler interval convention. This is a
conversion helper only: it does not automatically force simulations, modify a
\code{\link[=DemographicProcess]{DemographicProcess()}}, interpolate, or apply residuals to any model.
}
\details{
\code{residual_count} is interpreted as the total interval gap over
\verb{[time_start, time_end]}. For \code{use = "count"}, this interval total is
converted to the per-time additive flow used by \code{\link[=demographic_derivative]{demographic_derivative()}}:
\code{migration_count = residual_count / dt}. For \code{use = "rate"},
\code{residual_rate} is used directly as \code{migration_rate}. Schedule times are the
interval start times, \code{time_start}, matching agepi's exact-time Euler
schedule lookup semantics.

Missing residual rates are not replaced. If \code{use = "rate"} and any
\code{residual_rate} value is \code{NA_real_}, the function errors.
}
\examples{
ages <- AgeStructure(
  age_groups = c("0-4", "5+"),
  lower_bounds = c(0, 5),
  upper_bounds = c(4, Inf)
)
residual <- data.frame(
  time_start = c(0, 0),
  time_end = c(2, 2),
  dt = c(2, 2),
  age_group = c("0-4", "5+"),
  residual_count = c(10, -4),
  residual_rate = c(0.05, -0.01)
)
residual_to_migration_schedule(residual, ages, use = "count")
}
