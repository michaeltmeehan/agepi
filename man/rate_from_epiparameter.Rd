% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epiparameter.R
\name{rate_from_epiparameter}
\alias{rate_from_epiparameter}
\title{Convert an epiparameter delay to a Markov transition rate}
\usage{
rate_from_epiparameter(x)
}
\arguments{
\item{x}{An \verb{<epiparameter>} object, for example one returned by
\code{epiparameter::epiparameter_db()}.}
}
\value{
A finite positive numeric scalar equal to \code{1 / mean(x)}.
}
\description{
\code{rate_from_epiparameter()} extracts the mean delay from an
\verb{<epiparameter>} object and returns the corresponding per-capita Markov
transition rate, \code{1 / mean_delay}.
}
\details{
This is an exponential waiting-time approximation: it collapses the full
delay distribution represented by the \verb{<epiparameter>} object to a single
constant hazard with the same mean. It does not preserve the shape, variance,
truncation, or other distributional features of the original delay. Erlang
or gamma dwell-time approximations may be added in future work.
}
\examples{
if (requireNamespace("epiparameter", quietly = TRUE)) {
  incubation <- epiparameter::epiparameter_db(
    disease = "COVID-19",
    epi_name = "incubation period",
    single_epiparameter = TRUE
  )
  SEIRModel(
    sigma = rate_from_epiparameter(incubation),
    gamma = 0.25
  )
}
}
