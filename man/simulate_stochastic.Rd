% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_stochastic.R
\name{simulate_stochastic}
\alias{simulate_stochastic}
\title{Simulate a stochastic compartment model with Gillespie's direct method}
\usage{
simulate_stochastic(
  initial_state,
  times,
  model,
  age_structure,
  contact_matrix,
  beta = 1,
  susceptibility = NULL,
  infectiousness = NULL,
  population = NULL,
  method = "gillespie",
  seed = NULL,
  return_events = FALSE,
  demographic_process = NULL
)
}
\arguments{
\item{initial_state}{Long-form state data frame with columns \code{compartment},
\code{age_group}, and \code{value}, or numeric state vector.}

\item{times}{Numeric vector of finite, non-missing, strictly increasing time
points. Must have length at least two.}

\item{model}{Disease model. \code{SIRModel()}, \code{SEIRModel()}, and supported
\code{CompartmentModel()} outputs are currently supported.}

\item{age_structure}{Valid age structure.}

\item{contact_matrix}{Numeric contact matrix with rows as recipient age
groups and columns as source age groups.}

\item{beta}{Non-negative finite transmission scaling parameter.}

\item{susceptibility}{Optional non-negative numeric vector by recipient age
group.}

\item{infectiousness}{Optional non-negative numeric vector by source age
group.}

\item{population}{Optional fixed positive population vector by age group.
When supplied, it must match the age-specific totals in \code{initial_state}.
When omitted, age-specific totals from \code{initial_state} are used.}

\item{method}{Simulation method. Only \code{"gillespie"} is currently supported.}

\item{seed}{Optional integer seed for reproducible simulation.}

\item{return_events}{Logical scalar. If \code{FALSE}, return only the trajectory.
If \code{TRUE}, return a list with \code{trajectory} and \code{events}.}

\item{demographic_process}{Unsupported. Stochastic demography is not included
and must be \code{NULL}.}
}
\value{
If \code{return_events = FALSE}, a data frame with columns \code{time},
\code{compartment}, \code{age_group}, and \code{value}, ordered by time outermost,
compartment next, and age group innermost. If \code{return_events = TRUE}, a
list with \code{trajectory} and an event log containing \code{time}, \code{event},
\code{age_group}, \code{from}, \code{to}, and \code{rate}.
}
\description{
Runs a narrow stochastic simulation for supported age-structured
compartmental models: fixed population, no demography, no ageing, no
fertility, no mortality, no migration, and no tau-leaping.
}
\details{
The simulator uses Gillespie's continuous-time direct method. Infection
propensities reuse the package transition-rate interface. Each transition
rate row becomes one individual event type within an age group. Infection
propensities therefore use the existing force-of-infection convention: rows
of \code{contact_matrix} are recipient age groups and columns are infectious
source age groups.

The initial state may be supplied either as long-form state data or as a
numeric vector in the existing compartment-major, age-group-minor ordering.
Names on numeric state vectors are ignored, matching the state-mapping
helpers. Output rows are aligned to the requested \code{times}; if no event occurs
before an output time, the current state is carried forward.

If \code{seed} is supplied, the R random-number state is restored on exit when
possible, so the caller's RNG stream is not permanently advanced by this
function.
}
