% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demography.R
\name{demography_from_wpp}
\alias{demography_from_wpp}
\title{Construct demography from WPP-style tabular data}
\usage{
demography_from_wpp(
  data = NULL,
  dataset = NULL,
  age_structure,
  time_col,
  age_group_col,
  population_col,
  location = NULL,
  location_col = NULL
)
}
\arguments{
\item{data}{Optional data frame containing WPP-like population data.}

\item{dataset}{Optional dataset name to load from \code{wpp2024} when \code{data} is
not supplied.}

\item{age_structure}{Target age-structure object validated by
\code{\link[=validate_age_structure]{validate_age_structure()}}.}

\item{time_col, age_group_col, population_col}{Column names in \code{data}
containing time/year, age-group, and population values.}

\item{location}{Optional single country or location value to select.}

\item{location_col}{Optional column name containing country or location
values.}
}
\value{
An \code{agepi_demography} object.
}
\description{
Optional adapter for converting an already-loaded WPP-style table, or an
optional dataset from the \code{wpp2024} package, into the simple \code{time},
\code{age_group}, \code{population} table expected by \code{\link[=Demography]{Demography()}}. The external
package is not required for core agepi functionality when \code{data} is supplied
directly.
}
\details{
Inputs should be pre-filtered to one country, location, or other entity
before calling this adapter, or a single \code{location} should be selected with
\code{location_col}. When \code{location_col} is supplied and more than one location is
present, the adapter requires an explicit \code{location} rather than silently
mixing rows. Population values are interpreted as counts in the units
supplied by the caller; no scaling is applied.

This is only a data-shaping adapter. It does not implement projection
dynamics, interpolation, ageing, births, deaths, migration, fertility,
mortality, or any other modification of population trajectories.
}
\examples{
age_structure <- AgeStructure(
  age_groups = c("0-4", "5-9"),
  lower_bounds = c(0, 5),
  upper_bounds = c(4, 9)
)

wpp_like <- data.frame(
  year = c(2020, 2020, 2025, 2025),
  age = c("0-4", "5-9", "0-4", "5-9"),
  population = c(1000, 1200, 980, 1250),
  location = "Exampleland"
)

demography_from_wpp(
  data = wpp_like,
  age_structure = age_structure,
  time_col = "year",
  age_group_col = "age",
  population_col = "population",
  location = "Exampleland",
  location_col = "location"
)
}
