% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disease_model.R
\name{CompartmentModel}
\alias{CompartmentModel}
\title{Construct a generic age-structured compartment model}
\usage{
CompartmentModel(
  compartments,
  infection_transitions = NULL,
  transitions = NULL,
  outflows = NULL,
  infectious_compartments = NULL,
  infectiousness_weights = NULL,
  beta = NULL,
  birth_compartment = NULL,
  migration_compartment = NULL
)
}
\arguments{
\item{compartments}{Unique non-empty character vector of compartment names.}

\item{infection_transitions}{Data frame with columns \code{from} and \code{to}.
An optional \code{susceptibility} column may be supplied either as a numeric
scalar per row or as a list-column of numeric vectors. When omitted,
susceptibility defaults to one for every age group. Infection flow is
\code{lambda * state[from] * susceptibility} within each recipient age group.}

\item{transitions}{Optional data frame with columns \code{from}, \code{to}, and
\code{rate}, where \code{rate} is a non-negative per-capita transition rate. Each
rate may be a scalar or a named age-specific numeric vector in a list
column. Age-specific names are validated and reordered when transition
rates are evaluated against an \code{AgeStructure()}.}

\item{infectious_compartments}{Character vector naming compartment(s) that
contribute to infectious pressure. Defaults to \code{"I"} when present.}

\item{infectiousness_weights}{Optional non-negative numeric vector or list
giving relative infectiousness for \code{infectious_compartments}. A numeric
vector supplies one scalar per infectious compartment. A list can supply
one scalar or one age-specific numeric vector per infectious compartment.
When the list is named, names must match infectious compartments and are
reordered to match \code{infectious_compartments}. Defaults to one for each
infectious compartment.}

\item{beta}{Optional non-negative finite numeric scalar transmission rate.
The model stores this default for later use by simulation and
transition-rate evaluation. When omitted, infection-capable models default
to \code{1}; models without infection transitions leave the field \code{NULL}.}

\item{birth_compartment}{Optional compartment receiving demographic births.
Defaults to \code{"S"} when present.}

\item{migration_compartment}{Optional compartment receiving net migration
under \code{migration_policy = "susceptible"}. Defaults to \code{birth_compartment}.}
}
\value{
A \code{DiseaseModel} list describing a generic compartment model.
}
\description{
Creates a simple generic disease-model object for deterministic
age-structured compartmental models. Infection transitions use the existing
\code{\link[=force_of_infection]{force_of_infection()}} convention. Other transitions are per-capita flows
from \code{from} to \code{to}, with rates supplied either as finite non-negative
scalars or as age-specific vectors when transition rates are evaluated.
}
\details{
\code{SIRModel()} and \code{SEIRModel()} remain supported convenience constructors for
the built-in SIR and SEIR workflows. \code{CompartmentModel()} is intended for
explicit custom models with the same compartment-major state ordering.
}
\examples{
model <- CompartmentModel(
  compartments = c("S", "E", "I", "R"),
  infection_transitions = data.frame(from = "S", to = "E"),
  transitions = data.frame(
    from = c("E", "I"),
    to = c("I", "R"),
    rate = c(0.2, 0.25)
  ),
  infectious_compartments = "I"
)
}
