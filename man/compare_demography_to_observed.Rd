% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demography_validation.R
\name{compare_demography_to_observed}
\alias{compare_demography_to_observed}
\title{Compare simulated demography to observed population trajectories}
\usage{
compare_demography_to_observed(simulated, observed)
}
\arguments{
\item{simulated}{Data frame returned by \code{\link[=simulate_demography]{simulate_demography()}} with columns
\code{time}, \code{age_group}, and \code{population}.}

\item{observed}{An \code{agepi_demography} object from \code{\link[=Demography]{Demography()}} or
\code{\link[=demography_from_wpp]{demography_from_wpp()}}.}
}
\value{
A data frame with one row per shared exact time and age group, ordered
by time and then by the age-group order in \code{observed}. Columns are \code{time},
\code{age_group}, \code{simulated_population}, \code{observed_population},
\code{absolute_error}, \code{relative_error}, \code{simulated_total_population},
\code{observed_total_population}, \code{total_absolute_error},
\code{total_relative_error}, \code{simulated_age_share}, \code{observed_age_share}, and
\code{age_share_error}.
}
\description{
Diagnostic helper for comparing output from \code{\link[=simulate_demography]{simulate_demography()}} against
externally supplied population trajectories stored as an \code{\link[=Demography]{Demography()}} object,
including WPP-style objects created by \code{\link[=demography_from_wpp]{demography_from_wpp()}}.
}
\details{
The comparison is diagnostic only. It does not change simulation dynamics, add
residual forcing, interpolate between time points, transform age groups, or
imply that \code{\link[=simulate_demography]{simulate_demography()}} reproduces WPP projections. Only exact
shared time points and exact matching age groups are compared.

If \code{simulated} contains extra time points, they are ignored. If \code{observed}
contains extra time points, they are ignored. If there are no shared exact
time points, the function errors. Age structures must match exactly for this
first-pass diagnostic.
}
\examples{
ages <- AgeStructure(
  age_groups = c("0-4", "5+"),
  lower_bounds = c(0, 5),
  upper_bounds = c(4, Inf)
)

observed <- Demography(
  data.frame(
    time = c(2020, 2020, 2021, 2021),
    age_group = c("0-4", "5+", "0-4", "5+"),
    population = c(100, 200, 95, 210)
  ),
  ages
)

simulated <- data.frame(
  time = c(2020, 2020, 2021, 2021),
  age_group = c("0-4", "5+", "0-4", "5+"),
  population = c(100, 200, 97, 208)
)

compare_demography_to_observed(simulated, observed)
}
