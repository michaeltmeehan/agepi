% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demographic_residuals.R
\name{implied_demographic_residual}
\alias{implied_demographic_residual}
\title{Compute implied demographic residual flows}
\usage{
implied_demographic_residual(
  observed,
  process,
  type = c("both", "count", "rate")
)
}
\arguments{
\item{observed}{An \code{agepi_demography} object from \code{\link[=Demography]{Demography()}} or
\code{\link[=demography_from_wpp]{demography_from_wpp()}}.}

\item{process}{Valid \code{agepi_demographic_process} object.}

\item{type}{Which residual columns to include: \code{"count"}, \code{"rate"}, or
\code{"both"}.}
}
\value{
A data frame with one row per observed interval and age group,
ordered by \code{time_start} and the process age-group order. Common columns are
\code{time_start}, \code{time_end}, \code{dt}, \code{age_group}, \code{observed_start},
\code{observed_end}, \code{predicted_end}, and \code{model_derivative}. Residual columns
are controlled by \code{type}.
}
\description{
Diagnostic helper for one-step Euler accounting against an observed
demographic trajectory. For each observed interval, the function starts from
the observed population at \code{time_start}, evaluates \code{\link[=demographic_derivative]{demographic_derivative()}}
at that exact time using \code{process}, predicts the next population with one
Euler step, and reports the residual needed to match the observed population
at \code{time_end}.
}
\details{
The residual is diagnostic only. It is not automatically applied to a
process, converted to migration, used as residual forcing, or fed back into
simulation dynamics. No interpolation or age transformation is performed.
Residuals may be interpreted as implied unmodelled net flows, such as omitted
migration, data mismatch, or rate approximation error. If \code{process} already
includes migration, the residual is the remaining gap relative to that full
process.

For each age group and interval:

\code{predicted_end = observed_start + dt * model_derivative}

\code{residual_count = observed_end - predicted_end}

\code{residual_rate = residual_count / (dt * observed_start)}

\code{residual_rate} is \code{NA_real_} where \code{observed_start} is zero.
}
\examples{
ages <- AgeStructure(
  age_groups = c("0-4", "5+"),
  lower_bounds = c(0, 5),
  upper_bounds = c(4, Inf)
)
process <- build_demographic_process(ages)
observed <- Demography(
  data.frame(
    time = c(0, 0, 1, 1),
    age_group = c("0-4", "5+", "0-4", "5+"),
    population = c(100, 200, 85, 215)
  ),
  ages
)
implied_demographic_residual(observed, process)
}
