% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wpp_demographic_inputs.R
\name{standardise_wpp_migration}
\alias{standardise_wpp_migration}
\title{Standardise WPP-like migration inputs}
\usage{
standardise_wpp_migration(
  data,
  age_structure,
  time_col,
  age_col,
  migration_col,
  migration_type = c("count", "rate")
)
}
\arguments{
\item{data}{Data frame containing WPP-like migration data.}

\item{age_structure}{Target age-structure object validated by
\code{\link[=validate_age_structure]{validate_age_structure()}}.}

\item{time_col, age_col}{Column names containing time/year and age labels.}

\item{migration_col}{Column name containing age-specific migration values.}

\item{migration_type}{Whether \code{migration_col} contains \code{"count"} or \code{"rate"}
values.}
}
\value{
An \code{agepi_migration_schedule} object.
}
\description{
Converts a WPP-like age-specific migration table into a
\code{\link[=MigrationSchedule]{MigrationSchedule()}}. Migration values may be negative. Use
\code{migration_type = "count"} for net migrant counts and
\code{migration_type = "rate"} for annual per-person migration rates.
}
\details{
This helper is an adapter only: it standardises columns and age labels, then
delegates validation and storage to \code{\link[=MigrationSchedule]{MigrationSchedule()}}. It does not
implement projection dynamics, simulate demography, or guarantee reproduction
of WPP projections. The optional \code{wpp2024} package is not required when
supplying data directly.
}
\examples{
ages <- wpp_age_structure_5year(max_age = 10)
migration_like <- data.frame(
  year = rep(2020, ages$n_age_groups),
  age = ages$age_groups,
  net_migration = c(-10, 5, 2)
)
standardise_wpp_migration(
  migration_like,
  age_structure = ages,
  time_col = "year",
  age_col = "age",
  migration_col = "net_migration",
  migration_type = "count"
)
}
