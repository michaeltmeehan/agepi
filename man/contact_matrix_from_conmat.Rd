% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contact_matrix.R
\name{contact_matrix_from_conmat}
\alias{contact_matrix_from_conmat}
\title{Convert conmat-style output to an agepi contact matrix}
\usage{
contact_matrix_from_conmat(
  x,
  age_structure = NULL,
  orientation = c("recipient_source", "source_recipient"),
  transpose = FALSE
)
}
\arguments{
\item{x}{A conmat-style long table or object coercible with \code{as.data.frame()}.}

\item{age_structure}{Optional valid age structure used to validate dimensions
and age-group names.}

\item{orientation}{Orientation of the converted matrix. Use
\code{"recipient_source"} when rows are recipients and columns are sources. Use
\code{"source_recipient"} when rows are sources and columns are recipients.}

\item{transpose}{Logical scalar. If \code{TRUE}, transpose the matrix after
\code{orientation} handling.}
}
\value{
A numeric square matrix in agepi recipient-source orientation.
}
\description{
Optional, dependency-free adapter for conmat-style long tables. The \code{conmat}
package is not required for core agepi functionality. This adapter expects
\code{age_group_from}, \code{age_group_to}, and \code{contacts} columns, then delegates
conversion, validation, orientation handling, optional transposition, and
age-structure labelling to \code{\link[=as_agepi_contact_matrix]{as_agepi_contact_matrix()}}.
}
\details{
In conmat-style long input, \code{age_group_to} becomes recipient rows and
\code{age_group_from} becomes source columns. No interpolation, reciprocity
correction, population balancing, demographic prediction, or age-bin
splitting is applied.
}
\examples{
age_structure <- AgeStructure(
  age_groups = c("0-4", "5-9"),
  lower_bounds = c(0, 5),
  upper_bounds = c(4, 9)
)

conmat_like <- data.frame(
  age_group_from = c("0-4", "5-9", "0-4", "5-9"),
  age_group_to = c("0-4", "0-4", "5-9", "5-9"),
  contacts = c(4, 2, 1, 5)
)

contact_matrix_from_conmat(
  conmat_like,
  age_structure = age_structure
)
}
