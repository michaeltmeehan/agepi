% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contact_matrix.R
\name{as_agepi_contact_matrix}
\alias{as_agepi_contact_matrix}
\title{Coerce an object to an agepi contact matrix}
\usage{
as_agepi_contact_matrix(
  x,
  age_structure = NULL,
  orientation = c("recipient_source", "source_recipient"),
  transpose = FALSE
)
}
\arguments{
\item{x}{A supported contact-matrix input.}

\item{age_structure}{Optional valid age structure used to validate dimensions
and age-group names.}

\item{orientation}{Orientation of matrix-like inputs. Use
\code{"recipient_source"} when rows are recipients and columns are sources. Use
\code{"source_recipient"} when rows are sources and columns are recipients.}

\item{transpose}{Logical scalar. If \code{TRUE}, transpose the matrix after
\code{orientation} handling.}
}
\value{
A numeric square matrix in agepi recipient-source orientation.
}
\description{
Converts common contact-matrix inputs to the agepi convention used by
\code{\link[=force_of_infection]{force_of_infection()}}: rows are recipient age groups, columns are source age
groups, and \code{contact_matrix[a, b]} gives contacts made by recipient group
\code{a} with source group \code{b}.
}
\details{
Supported inputs are numeric matrices, data frames that can be converted to
numeric matrices, socialmixr-like list objects with a numeric matrix at
\code{x$matrix}, and conmat-style long data frames with \code{age_group_from},
\code{age_group_to}, and \code{contacts} columns. For conmat-style long data,
\code{age_group_to} is used for recipient rows and \code{age_group_from} is used for
source columns.

Orientation is handled before the explicit \code{transpose} argument. That is,
\code{orientation = "source_recipient"} first transposes the coerced matrix into
recipient-source orientation, then \code{transpose = TRUE} transposes that result.
}
