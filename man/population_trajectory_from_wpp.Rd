% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demography.R
\name{population_trajectory_from_wpp}
\alias{population_trajectory_from_wpp}
\title{Build a projection-backed population trajectory from WPP-style data}
\usage{
population_trajectory_from_wpp(
  data,
  age_structure,
  location,
  years,
  location_col = "name",
  time_col = "year",
  age_group_col = "age",
  population_col = "pop"
)
}
\arguments{
\item{data}{Data frame containing WPP-like age-specific population
projections.}

\item{age_structure}{Target age-structure object validated by
\code{\link[=validate_age_structure]{validate_age_structure()}}.}

\item{location}{Single location/country value to select.}

\item{years}{Numeric vector of projection years to include.}

\item{location_col, time_col, age_group_col, population_col}{Column names in
\code{data} containing location, year/time, age group, and population values.}
}
\value{
Data frame with columns \code{time}, \code{age_group}, and \code{population},
ordered by requested year and \code{age_structure$age_groups}.
}
\description{
Converts an already-loaded WPP-style annual age-specific population
projection into the standard agepi demography trajectory table with columns
\code{time}, \code{age_group}, and \code{population}. This helper replays an external
projection as supplied by the caller; it does not simulate births, deaths,
ageing, fertility, mortality, migration, sex structure, or cohort-component
assumptions.
}
\details{
The main WPP use case is \code{wpp2024::popprojAge1dt} with
\code{wpp_age_structure_1year(max_age = 100)}. The \code{wpp2024} package is not
required when \code{data} is supplied directly.
}
\examples{
ages <- wpp_age_structure_1year(max_age = 3)
wpp_like <- data.frame(
  name = "Kiribati",
  year = rep(2020:2021, each = ages$n_age_groups),
  age = rep(c(0, 1, 2, "3+"), times = 2),
  pop = c(100, 95, 90, 85, 101, 96, 91, 86)
)

population_trajectory_from_wpp(
  wpp_like,
  age_structure = ages,
  location = "Kiribati",
  years = 2020:2021,
  population_col = "pop"
)
}
