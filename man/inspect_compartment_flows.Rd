% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_inspection.R
\name{inspect_compartment_flows}
\alias{inspect_compartment_flows}
\title{Inspect inflows and outflows for model compartments}
\usage{
inspect_compartment_flows(model, compartment = NULL)
}
\arguments{
\item{model}{Disease model created by \code{\link[=SIRModel]{SIRModel()}}, \code{\link[=SEIRModel]{SEIRModel()}}, or
\code{\link[=CompartmentModel]{CompartmentModel()}}.}

\item{compartment}{Optional compartment name to inspect in detail.}
}
\value{
When \code{compartment = NULL}, a data frame with one row per compartment
and columns \code{compartment}, \code{n_inflows}, \code{n_outflows}, \code{inflow_transition_ids},
\code{outflow_transition_ids}, \code{inflow_sources}, and \code{outflow_destinations}.
When \code{compartment} is supplied, a transition-level data frame with columns
\code{direction}, \code{transition_id}, \code{source}, \code{destination}, \code{transition_type},
and \code{rate_definition}.
}
\description{
Summarises how each compartment participates in the model transition graph,
or, when \code{compartment} is supplied, returns the transition-level inflow and
outflow table for that compartment.
}
\examples{
model <- CompartmentModel(
  compartments = c("S", "I", "R"),
  infection_transitions = data.frame(from = "S", to = "I"),
  transitions = data.frame(from = "I", to = "R", rate = 0.2)
)
inspect_compartment_flows(model)
inspect_compartment_flows(model, "I")
}
