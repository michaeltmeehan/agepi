% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demographic_process_builder.R
\name{build_demographic_process}
\alias{build_demographic_process}
\title{Build a demographic process from schedules}
\usage{
build_demographic_process(
  age_structure,
  fertility_schedule = NULL,
  fertility_exposure_fraction = 1,
  mortality_schedule = NULL,
  migration_schedule = NULL,
  mode = c("closed", "migration")
)
}
\arguments{
\item{age_structure}{Age structure validated by \code{\link[=validate_age_structure]{validate_age_structure()}}.}

\item{fertility_schedule}{Optional \code{\link[=FertilitySchedule]{FertilitySchedule()}} object.}

\item{fertility_exposure_fraction}{Single non-negative finite multiplier
applied to fertility exposure when computing births. Defaults to \code{1}.}

\item{mortality_schedule}{Optional \code{\link[=MortalitySchedule]{MortalitySchedule()}} object.}

\item{migration_schedule}{Optional \code{\link[=MigrationSchedule]{MigrationSchedule()}} object.}

\item{mode}{Process mode, either \code{"closed"} or \code{"migration"}.}
}
\value{
An \code{agepi_demographic_process} object.
}
\description{
Convenience builder for combining an age structure and already-constructed
demographic schedule objects into a \code{\link[=DemographicProcess]{DemographicProcess()}}. The builder
constructs \code{\link[=AgeingOperator]{AgeingOperator()}} from \code{age_structure} and delegates validation
to \code{\link[=DemographicProcess]{DemographicProcess()}}.
}
\details{
This function does not parse WPP data, simulate demography, infer missing
fertility, mortality, or migration schedules, implement interpolation, or
implement residual forcing. It is only a small assembly helper for schedule
objects that have already been standardised or constructed.
}
\examples{
ages <- wpp_age_structure_5year(max_age = 10)
process <- build_demographic_process(ages)
process$mode

mortality <- MortalitySchedule(
  data.frame(
    time = 2020,
    age_group = ages$age_groups,
    mortality_rate = rep(0.01, ages$n_age_groups)
  ),
  ages
)
build_demographic_process(
  age_structure = ages,
  mortality_schedule = mortality
)
}
