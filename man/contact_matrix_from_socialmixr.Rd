% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contact_matrix.R
\name{contact_matrix_from_socialmixr}
\alias{contact_matrix_from_socialmixr}
\title{Convert a socialmixr contact matrix output to an agepi contact matrix}
\usage{
contact_matrix_from_socialmixr(
  x,
  age_structure = NULL,
  orientation = c("recipient_source", "source_recipient"),
  transpose = FALSE
)
}
\arguments{
\item{x}{A numeric matrix or socialmixr contact-matrix-like list object with
a numeric \code{matrix} element.}

\item{age_structure}{Optional valid age structure used to validate dimensions
and age-group names.}

\item{orientation}{Orientation of the extracted matrix. Use
\code{"recipient_source"} when rows are recipients and columns are sources. Use
\code{"source_recipient"} when rows are sources and columns are recipients.}

\item{transpose}{Logical scalar. If \code{TRUE}, transpose the matrix after
\code{orientation} handling.}
}
\value{
A numeric square matrix in agepi recipient-source orientation.
}
\description{
Optional, dependency-free adapter for objects shaped like the output of
\code{socialmixr::contact_matrix()}. The \code{socialmixr} package is not required for
core agepi functionality. This adapter accepts either a numeric matrix or a
list-like object with a numeric \code{matrix} element, then delegates validation,
orientation handling, optional transposition, and age-structure labelling to
\code{\link[=as_agepi_contact_matrix]{as_agepi_contact_matrix()}}.
}
\details{
Returned matrices use the agepi convention: rows are recipient age groups and
columns are source age groups. No interpolation, reciprocity correction,
population balancing, or age-bin splitting is applied.
}
\examples{
age_structure <- AgeStructure(
  age_groups = c("0-4", "5-9"),
  lower_bounds = c(0, 5),
  upper_bounds = c(4, 9)
)

socialmixr_like <- list(
  matrix = matrix(
    c(4, 2,
      1, 5),
    nrow = 2,
    byrow = TRUE
  )
)

contact_matrix_from_socialmixr(
  socialmixr_like,
  age_structure = age_structure
)
}
