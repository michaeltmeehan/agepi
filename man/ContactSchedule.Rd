% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contact_matrix.R
\name{ContactSchedule}
\alias{ContactSchedule}
\title{Construct a contact schedule}
\usage{
ContactSchedule(data, age_structure)
}
\arguments{
\item{data}{Either a named list of contact matrices with names coercible to
numeric times, or a long data frame with columns \code{time}, \code{age_group_from},
\code{age_group_to}, and \code{contacts}.}

\item{age_structure}{Age-structure object validated by
\code{\link[=validate_age_structure]{validate_age_structure()}}.}
}
\value{
An \code{agepi_contact_schedule} list.
}
\description{
Stores externally supplied contact matrices indexed by time. Matrices use the
agepi force-of-infection convention: rows are recipient age groups and
columns are source age groups. No interpolation, reciprocity correction, or
population balancing is applied.
}
\examples{
age_structure <- AgeStructure(
  age_groups = c("0-4", "5-9"),
  lower_bounds = c(0, 5),
  upper_bounds = c(4, 9)
)

contacts <- list(
  "0" = matrix(c(4, 2, 1, 5), nrow = 2, byrow = TRUE),
  "1" = matrix(c(3, 2, 2, 4), nrow = 2, byrow = TRUE)
)

schedule <- ContactSchedule(contacts, age_structure)
contact_matrix_at(schedule, time = 1)
}
