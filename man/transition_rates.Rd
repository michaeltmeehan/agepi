% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition_rates.R
\name{transition_rates}
\alias{transition_rates}
\title{Compute disease transition rates}
\usage{
transition_rates(
  state,
  model,
  age_structure,
  contact_matrix,
  beta = 1,
  susceptibility = NULL,
  infectiousness = NULL
)
}
\arguments{
\item{state}{Long-form state data frame or numeric state vector.}

\item{model}{Disease model. \code{SIRModel()}, \code{SEIRModel()}, and
\code{CompartmentModel()} output are supported.}

\item{age_structure}{Valid age structure.}

\item{contact_matrix}{Numeric contact matrix with rows as recipient age
groups and columns as source age groups.}

\item{beta}{Non-negative finite transmission scaling parameter.}

\item{susceptibility}{Optional non-negative numeric vector by recipient age
group.}

\item{infectiousness}{Optional non-negative numeric vector by source age
group.}
}
\value{
Data frame with columns \code{from}, \code{to}, \code{age_group}, \code{rate}, and
\code{transition_id}, ordered by age group outermost and transition innermost.
\code{transition_id} identifies the logical transition and is shared by all age
rows for that transition.
}
\description{
Computes transition hazards/flows for a disease model at the supplied state.
The returned rates are not updated compartment counts. SIR and SEIR disease
models and generic \code{CompartmentModel()} models are supported.
}
\details{
\code{state} may be either a long-form data frame with columns \code{compartment},
\code{age_group}, and \code{value}, or a numeric vector using the existing
compartment-major, age-group-minor ordering. Names on numeric state vectors
are ignored.
}
