% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_inspection.R
\name{check_population_balance}
\alias{check_population_balance}
\title{Check population balance from evaluated disease-model transitions}
\usage{
check_population_balance(
  state,
  model,
  age_structure,
  contact_matrix,
  ...,
  tolerance = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{state}{Long-form state data frame or numeric state vector.}

\item{model}{Disease model created by \code{\link[=SIRModel]{SIRModel()}}, \code{\link[=SEIRModel]{SEIRModel()}}, or
\code{\link[=CompartmentModel]{CompartmentModel()}}.}

\item{age_structure}{Valid age structure.}

\item{contact_matrix}{Numeric contact matrix with rows as recipient age
groups and columns as source age groups.}

\item{...}{Additional arguments forwarded to \code{\link[=transition_rates]{transition_rates()}}.}

\item{tolerance}{Numeric scalar tolerance for the balance check.}
}
\value{
A list with \code{summary} and \code{by_age} data frames.
}
\description{
Evaluates transition rates, reconstructs compartment derivatives, and
compares the total derivative against the net external contribution implied
by the evaluated transition table.
}
\details{
This check covers disease-model transitions only. It does not include a
separate demographic process, ageing operator, or other simulation-side
adjustments. External inflows are only counted when they are represented in
the evaluated transition table; the current disease-model transition
machinery primarily represents external outflows via \code{to = NA}.
}
\examples{
ages <- AgeStructure(
  age_groups = c("0-4", "5-9"),
  lower_bounds = c(0, 5),
  upper_bounds = c(4, 9)
)
model <- SIRModel(gamma = 0.2)
state <- data.frame(
  compartment = rep(c("S", "I", "R"), each = 2),
  age_group = rep(ages$age_groups, times = 3),
  value = c(95, 90, 5, 10, 0, 0)
)
check_population_balance(state, model, ages, diag(2), beta = 0.1)
}
