% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wpp_demographic_inputs.R
\name{fertility_from_wpp_percent_asfr}
\alias{fertility_from_wpp_percent_asfr}
\title{Convert WPP-style percent ASFR weights to fertility rates}
\usage{
fertility_from_wpp_percent_asfr(
  data,
  age_structure,
  time_col,
  age_col,
  weight_col,
  tfr_data,
  tfr_time_col,
  tfr_col,
  weight_type = c("percent", "fraction"),
  maternal_age_groups = NULL,
  tolerance = 1e-06
)
}
\arguments{
\item{data}{Data frame containing WPP-like percent-ASFR or fraction-ASFR
weights.}

\item{age_structure}{Target age-structure object validated by
\code{\link[=validate_age_structure]{validate_age_structure()}}.}

\item{time_col, age_col, weight_col}{Column names containing time/year, maternal
age labels, and age-specific fertility weights.}

\item{tfr_data}{Data frame containing total fertility rates by time. Inputs
should be pre-filtered to one country, location, or other entity before
calling this adapter.}

\item{tfr_time_col, tfr_col}{Column names in \code{tfr_data} containing time/year
and total fertility rate values.}

\item{weight_type}{Whether \code{weight_col} contains \code{"percent"} values summing
to 100 or \code{"fraction"} values summing to 1 within each time point.}

\item{maternal_age_groups}{Character vector of age groups that must be present
at every time point. Defaults to WPP's usual five-year reproductive ages
from 15-19 through 45-49 when those labels are present in \code{age_structure},
otherwise all age groups appearing in \code{data}.}

\item{tolerance}{Numeric tolerance for checking weight sums.}
}
\value{
An \code{agepi_fertility_schedule} object with annual births per female
person-year.
}
\description{
Converts WPP 2024 \code{percentASFR1dt}-style fertility schedules into an
\code{\link[=FertilitySchedule]{FertilitySchedule()}}. The supplied age-specific values are interpreted as a
distribution of total fertility across maternal age groups, not as
per-capita fertility rates. For each time and maternal age bin, the returned
annual fertility rate is \code{TFR * fraction / age_bin_width}, where percentages
are first divided by 100. Maternal age bins must have finite widths;
open-ended maternal bins are rejected.
}
\details{
This is an adapter only. It does not implement WPP projection dynamics,
interpolation, cohort fertility, sex-specific exposure correction, or any
complete WPP projection system.
}
\examples{
ages <- wpp_age_structure_5year()
weights <- data.frame(
  year = rep(2020, 7),
  age = c("15-19", "20-24", "25-29", "30-34", "35-39", "40-44", "45-49"),
  percent_asfr = c(5, 20, 30, 25, 15, 4, 1)
)
tfr <- data.frame(year = 2020, tfr = 2.1)
fertility_from_wpp_percent_asfr(
  weights,
  age_structure = ages,
  time_col = "year",
  age_col = "age",
  weight_col = "percent_asfr",
  tfr_data = tfr,
  tfr_time_col = "year",
  tfr_col = "tfr"
)
}
