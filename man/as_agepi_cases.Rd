% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/case_data.R
\name{as_agepi_cases}
\alias{as_agepi_cases}
\title{Map observed case data to agepi age groups}
\usage{
as_agepi_cases(cases, age_structure, age_col = NULL, age_group_col = NULL)
}
\arguments{
\item{cases}{A \code{data.frame} with one row per observed case or event.}

\item{age_structure}{An \code{\link[=AgeStructure]{AgeStructure()}} object.}

\item{age_col}{Optional name of a numeric exact-age column in \code{cases}.}

\item{age_group_col}{Optional name of an existing age-group column in \code{cases}.}
}
\value{
A \code{data.frame} containing all original columns plus a validated
\code{age_group} column ordered as \code{age_structure$age_groups}. If \code{cases} is a
\code{linelist} object and the \code{linelist} package is installed, existing
linelist tags are preserved where possible.
}
\description{
Prepares a user-supplied case or event data frame for aggregate agepi
workflows by validating or deriving an \code{age_group} column against an
\code{\link[=AgeStructure]{AgeStructure()}}. This helper only maps observed records into model age bins;
it does not create individual-level line lists from agepi compartment
trajectories.
}
\examples{
ages <- AgeStructure(c("0-4", "5-9", "10+"), c(0, 5, 10), c(4, 9, Inf))
observed_cases <- data.frame(
  case_id = c("a", "b", "c", "d"),
  onset_day = c(1, 1, 2, 2),
  age = c(2, 7, 12, 41)
)

mapped_cases <- as_agepi_cases(
  observed_cases,
  age_structure = ages,
  age_col = "age"
)

aggregate(case_id ~ onset_day + age_group, mapped_cases, length)
}
