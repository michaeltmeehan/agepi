% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_demography.R
\name{simulate_demography}
\alias{simulate_demography}
\title{Simulate demographic-only one-compartment population dynamics}
\usage{
simulate_demography(
  process,
  initial_state,
  times,
  method = c("euler", "deSolve", "ode"),
  ...
)
}
\arguments{
\item{process}{Valid \code{agepi_demographic_process} object.}

\item{initial_state}{Numeric vector of non-negative population counts ordered
by \code{process$age_structure$age_groups}. Names are ignored on input and
restored to the process age groups internally.}

\item{times}{Numeric vector of finite, non-missing, strictly increasing time
points. Must have length at least two.}

\item{method}{Simulation method. \code{"euler"} is the default. \code{"deSolve"} and
\code{"ode"} are accepted for future compatibility but currently error for this
demographic wrapper.}

\item{...}{Reserved for future method-specific arguments. Currently unused.}
}
\value{
Data frame with columns \code{time}, \code{age_group}, and \code{population},
ordered by time outermost and process age groups innermost.
}
\description{
Simulates one-compartment age-structured population dynamics from an
\code{\link[=DemographicProcess]{DemographicProcess()}} using \code{\link[=demographic_derivative]{demographic_derivative()}}. This is a
demographic-only wrapper: it does not implement infection dynamics,
infection-demography coupling, residual forcing, WPP validation diagnostics,
or WPP parsing.
}
\details{
Schedule lookup is exact-time only. No interpolation is performed. With the
Euler method, schedules are evaluated at the left endpoint of each interval,
\code{times[i]}. If a required schedule is not available at that exact time,
simulation errors through \code{\link[=demographic_derivative]{demographic_derivative()}}. The optional
\code{method = "deSolve"} and its alias \code{method = "ode"} are reserved for future
support and currently error clearly because adaptive solver time points are
not compatible with exact-time schedules without interpolation or another
explicit time-handling rule.

Euler updates are not truncated: if an update would produce a negative
population value, simulation stops with an error.
}
\examples{
ages <- AgeStructure(
  age_groups = c("0-4", "5+"),
  lower_bounds = c(0, 5),
  upper_bounds = c(4, Inf)
)
process <- DemographicProcess(age_structure = ages)
simulate_demography(
  process = process,
  initial_state = c(100, 50),
  times = c(2020, 2021)
)
}
