% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{CalibrationTarget}
\alias{CalibrationTarget}
\title{Calibration target for already-run simulation outputs}
\usage{
CalibrationTarget(
  name,
  data,
  type = c("trajectory", "cumulative"),
  observation_model = c("gaussian", "lognormal", "poisson"),
  compartment = NULL,
  cumulative_name = NULL,
  transition_id = NULL,
  from = NULL,
  to = NULL,
  sigma = NULL,
  sd = NULL,
  se = NULL,
  sdlog = NULL,
  weight = 1,
  value_column = "value"
)
}
\arguments{
\item{name}{Character scalar target name.}

\item{data}{Observed data frame. Must contain \code{time} and \code{value}; may contain
\code{age_group}. Gaussian targets need an uncertainty from \code{sd}, \code{se}, or
\code{sigma}. Lognormal targets need \code{sdlog} or \code{sigma}.}

\item{type}{Output source, either \code{"trajectory"} or \code{"cumulative"}.}

\item{observation_model}{Observation family: \code{"gaussian"}, \code{"lognormal"}, or
\code{"poisson"}.}

\item{compartment}{Compartment to select for trajectory targets.}

\item{cumulative_name, transition_id, from, to}{Optional cumulative-flow
selectors for cumulative targets.}

\item{sigma, sd, se, sdlog}{Optional scalar observation uncertainty. These are
used when the corresponding column is absent from \code{data}.}

\item{weight}{Numeric scalar multiplier for this target contribution.}

\item{value_column}{Name of the observed value column in \code{data}.}
}
\value{
An S3 list with class \code{"agepi_calibration_target"}.
}
\description{
Defines one observed data target to compare against an existing deterministic
trajectory or cumulative-flow simulation output. This first calibration
scaffold evaluates simulation outputs only; it does not run simulations,
inject parameters, optimise, or sample.
}
\examples{
target <- CalibrationTarget(
  name = "infectious",
  data = data.frame(time = c(0, 1), value = c(10, 12), sd = c(1, 1)),
  type = "trajectory",
  observation_model = "gaussian",
  compartment = "I"
)
}
