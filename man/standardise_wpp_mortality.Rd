% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wpp_demographic_inputs.R
\name{standardise_wpp_mortality}
\alias{standardise_wpp_mortality}
\title{Standardise WPP-like mortality inputs}
\usage{
standardise_wpp_mortality(
  data,
  age_structure,
  time_col,
  age_col,
  mortality_col
)
}
\arguments{
\item{data}{Data frame containing WPP-like mortality data.}

\item{age_structure}{Target age-structure object validated by
\code{\link[=validate_age_structure]{validate_age_structure()}}.}

\item{time_col, age_col, mortality_col}{Column names containing time/year, age
labels, and mortality rates.}
}
\value{
An \code{agepi_mortality_schedule} object.
}
\description{
Converts a WPP-like age-specific mortality table into a
\code{\link[=MortalitySchedule]{MortalitySchedule()}}. This helper is an adapter only: it standardises
columns and age labels, then delegates validation and storage to
\code{\link[=MortalitySchedule]{MortalitySchedule()}}. It does not implement projection dynamics, simulate
demography, or guarantee reproduction of WPP projections. The optional
\code{wpp2024} package is not required when supplying data directly.
}
\details{
The mortality input is interpreted as an annual hazard, compatible with the
\code{\link[=MortalitySchedule]{MortalitySchedule()}} convention. WPP central death rates (\code{mx}) may be used
as a first-pass annual hazard approximation.
}
\examples{
ages <- wpp_age_structure_5year(max_age = 10)
mortality_like <- data.frame(
  year = rep(2020, ages$n_age_groups),
  age = ages$age_groups,
  mx = c(0.01, 0.002, 0.03)
)
standardise_wpp_mortality(
  mortality_like,
  age_structure = ages,
  time_col = "year",
  age_col = "age",
  mortality_col = "mx"
)
}
